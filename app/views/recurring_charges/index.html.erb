<%= javascript_pack_tag 'RecurringChargesLink' %>
<%= javascript_pack_tag 'SettingsPage' %>
<%=  link_to 'remove google analytics acct', google_auth_destroy_path, method: :delete, data: { confirm: 'This is something you should pay attention to?' } %>

<% @recurring_charges.each do |charge| %>
  <div>
    <h2><%= charge.name %></h2>
    <h3><%= charge.price %></h3>
    <h4><%= charge.billing_on %></h4>
    <%= link_to 'Cancel Charge', recurring_charge_path(charge), method: :delete %>
  </div>
<% end %>

<%= content_tag :div,
  id: "settings-page",
  data: { recurring_charges: @recurring_charges,
          google_api_id: @google_api_id
}.to_json do %>
<% end %>